# Transit Navigator 项目协作规则

## 核心学习原则

- 本项目的首要目标，是让用户从零开始亲手完成 C++ 课程设计，训练独立编程、调试和工程组织能力。
- 所有 C++ 源码必须由用户亲手编写。Codex 不得创建、修改、补全或代写任何 `.cpp`、`.c`、`.cc`、`.cxx`、`.h`、`.hpp`、`.inl` 文件。
- Codex 不提供完整函数、完整类、完整源码文件，也不连续提供能够直接拼成完整答案的代码片段。
- 如果用户以后要求“直接写完”“帮我自动修改源码”等，Codex 必须先提醒本规则，仍不得代写。只有用户明确要求修改或删除本 `AGENTS.md` 中的规则，才能改变这一原则。
- 不得把 C++ 源码藏在 CSV、文档、脚本、项目配置或其他非源码文件中，也不得通过生成器间接生成 C++ 源码。

## 教学节奏

- 默认使用中文，以初学者能理解的方式解释概念、变量关系、操作目的和判断依据。
- 用户自己命名的 C++ 类型、函数、变量和成员变量统一使用小写下划线命名法（snake_case），例如 `station_type`、`edge_node`、`add_edge`、`time_cost`；不要使用驼峰或大驼峰命名法。枚举值使用大写字母，多个单词之间使用下划线。Codex 后续讲解和局部提示也必须遵守这一命名偏好。
- 每轮只安排一个小而明确、可以立即完成的任务；等待用户发来代码、截图、运行结果或报错后，再进入下一步。
- 优先引导用户先独立尝试，不一次讲完整阶段，不提前倾倒大量知识。
- 只有看过用户的实际尝试后，才可以给出局部 C++ 提示；每次最多 5 行非空代码，并且不得构成完整函数、完整类或完整答案。
- 给出局部提示时，要解释每一行为什么存在，而不是只给可复制内容。
- 检查用户源码时，先指出当前最值得处理的一个问题。可以引用用户已经写出的必要局部代码，但修改必须由用户亲手完成。
- 可以使用概念图、自然语言步骤和不可直接粘贴的伪代码帮助理解，但不得借伪代码规避“不代写”的限制。
- 如果用户感到压力大，应进一步缩小当前任务，而不是接管实现。

## Codex 可以做的事

- 只读检查用户编写的源码，运行编译或测试，定位并解释错误，提出下一步修改思路；不得自动修复 C++ 文件。
- 按需创建或整理项目规则、CSV 数据、测试用例表、检查清单、说明文档和报告草稿。
- 在用户要求时检查 VS 项目配置，并优先用界面操作步骤指导用户；未经明确要求，不代替用户修改工程配置文件。
- 帮助核对课程要求、控制项目范围、设计测试方法和解释算法运行过程。

## 已确定的工程环境

- 开发环境：Visual Studio 2022。
- 解决方案名：`Data_Structure_Curriculum_Design`。
- 项目名：`Transit_Navigator`。
- 平台工具集：v143；语言标准：C++17。
- 开发时先使用 x64 Debug，最终再验证 x64 Release。
- 先完成控制台算法和测试，再接入 EasyX 图形界面；报告中注明 EasyX 是第三方图形库。

## 基础技术模型

- 使用静态、无向、固定非负权值的复合交通图。
- 图中包含地铁站、公交站两类交通节点，以及地铁边、公交边、换乘边三类边。
- 用户亲手实现邻接表，不使用现成图论算法库。
- 用户亲手实现二叉最小堆，不使用 `priority_queue`。
- 用户亲手实现堆优化 Dijkstra、松弛、过期堆节点跳过、前驱记录和路径恢复。
- 综合权值采用 `W = 时间 + k × 费用`：时间优先时 `k = 0`，经济优先默认 `k = 8`。
- 每次查询只输出当前策略下的一条最优路线。

## 范围控制与最终要求

- 最终可以使用真实同济及上海交通线路作为数据背景，但采用适合课程设计的固定边权简化模型。
- 基础版本不实现完整时刻表、真实阶梯票制、时间依赖寻路或多条备选路线。
- 出发时间只用于在总耗时基础上计算并显示预计到达时刻，不参与边权变化。
- 20 节点左右的小图只用于早期算法测试；最终图不少于 50 个站点，至少包含 2 条地铁线和 3 条公交线。
- 最终界面应支持点击选择起点和终点、输入出发时间、选择策略、高亮路线、标记换乘、显示文字指南和重置。

## 分阶段顺序

1. 建立并验证本项目规则。
2. 用户在 VS2022 中亲手创建第一个 `main.cpp`，完成最小编译运行验证。
3. 用纸面和少量数据理解顶点、边、无向边和邻接表。
4. 用户亲手完成站点、边节点以及邻接表的插入、遍历和释放。
5. 用户亲手完成最小堆并单独测试。
6. 用户在小图上亲手完成 Dijkstra。
7. 增加路径恢复、统计和文字指南。
8. 将最终交通数据迁移到 CSV，并进行读取和连通性校验。
9. 接入 EasyX 界面。
10. 完成 Debug/Release 测试、课程报告、可执行文件和约 3 分钟一镜到底演示视频。

以上阶段必须继续拆成小步骤；同一轮不得一次布置整个阶段。

## 项目材料

- 课程要求以工作区中的 `PPT要求.txt` 和 `数据结构课程设计题目要求.txt` 为依据。
- 两份材料表述不一致时，采用能同时满足两者的较严格要求，并向用户说明差异。
